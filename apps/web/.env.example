# PatchGuard frontend configuration. Copy to .env.

# ERP backend (auth, contractors, inspections, actions, reports, damage-report).
VITE_API_BASE=http://localhost:8000

# Agent control plane (job creation + live WebSocket inspection events).
VITE_AGENT_BASE=http://localhost:8765
